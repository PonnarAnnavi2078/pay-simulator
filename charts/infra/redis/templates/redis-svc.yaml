{{- if .Values.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
  labels:
    app: redis

spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

{{- end }}