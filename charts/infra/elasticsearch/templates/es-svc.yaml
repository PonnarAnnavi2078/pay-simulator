{{- if .Values.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-elasticsearch
  labels:
    app: elasticsearch

spec:
  clusterIP: None
  selector:
    app: elasticsearch
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.httpPort }}

{{- end }}