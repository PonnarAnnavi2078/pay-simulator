{{- if .Values.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svtrest
spec:
  type: {{ .Values.service.type }}
  selector:
    app: svtrest-simulator
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

{{- end }}